management:
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
  server:
    port: 8081
server:
  error:
    whitelabel:
      enabled: false
spring:
  application:
    name: blog
contentful:
  baseUrl: ${CONTENTFUL_BASE_URL:https://cdn.contentful.com}
  token: ${CONTENTFUL_TOKEN:no-token}
  space: ${CONTENTFUL_SPACE:no-space}
  environment: ${CONTENTFUL_ENVIRONMENT:no-env}
