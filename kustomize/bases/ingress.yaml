apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blog
  annotations:
    cert-manager.io/issuer: letsencrypt # Will generate Certificate
spec:
  ingressClassName: public
  rules:
    - host: blog.api.harmelodic.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: blog
                port:
                  name: http
  tls:
    - hosts:
        - harmelodic.com
      secretName: tls-cert-blog-api-harmelodic-com # Secret will be auto-generated by cert-manager
